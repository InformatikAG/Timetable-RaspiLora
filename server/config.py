SERVER = "tipo.webuntis.com"
USERNAME = "username"
PASSWORD = "password"
SCHOOL = "TBZ Mitte Bremen"

# room name -> device id
ROOMS = {
    "2.312": 1,
}

# delay to wait after hibernation
# and before sending the request hibernation request (after drawing the new buffer)
HIBERNATION_DELAY = 10
